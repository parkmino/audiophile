#!/bin/sh

usage () {
 echo "Usage: $(basename "$0") on/off/scan"
 echo "Run GMediaServer as UPnP Media Server"
}

case $1 in
 on)   /dev/shm/nohup gmediaserver -b --disable-tags --no-timestamp --fs-charset=UTF-8 -v0 --profile=generic -ienp3s0 --file-types=unknown /home/parkmino/Music/ /home/parkmino/Videos/>/dev/null</dev/null 2>/dev/null ;;
 off)  pkill gmediaserver ;;
 scan) kill -s USR1 $(pgrep -x gmediaserver) ;;
 *)    usage
esac
