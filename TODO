1. Install shairport-sync in raspbian

	sudo apt-get install shairport-sync
	
	if it does not work,
	
	wget http://ftp.us.debian.org/debian/pool/main/s/shairport-sync/shairport-sync_2.8.0-1_armhf.deb
	sudo dpkg -i shairport-sync*.deb
	
	and check any depedecies and install them.