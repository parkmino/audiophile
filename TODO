1. Comment out all the vm.* kernel configs.

2. sudo nano /usr/share/alsa/alsa.conf
	#pcm.shm.nonblock 1

3. sudo nano /etc/mpd.conf
	#always_on           "yes"
	#replay_gain_handler "none"

4. Same taskset in rygel
