1. sudo nano /usr/share/alsa/alsa.conf
	#ctl.default.type hw
	#ctl.default.card -1

2. dpkg -l pulseaudio
	sudo apt-get purge pulseaudio

3. lspci -nn
	sudo setpci -v -d xxxx:xxxx latency_timer=ff

4. sudo nano /boot/cmdline.txt
	smsc95xx.turbo_mode=N
