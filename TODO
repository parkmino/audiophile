1. sudo mv /usr/share/alsa/alsa.conf     /usr/share/alsa/alsa.conf.min
   sudo mv /usr/share/alsa/alsa.conf.org /usr/share/alsa/alsa.conf
   sudo nano /usr/share/alsa/alsa.conf
   	#defaults.pcm.nonblock 1
	#defaults.pcm.compat 0
	#defaults.pcm.minperiodtime 5000                # in us
   sudo nano /etc/mpd.conf
	audio_output {
	device			"hw:0,0,0"
	auto_resample		"no"
	auto_channels		"no"
	auto_format		"no"
	mixer_type		"none"
	#mixer_device		"hw:0"
	#mixer_control		"XMOS Clock Selector"
	#replay_gain_handler	"none"
	}
	#mixer_type		"disabled"
