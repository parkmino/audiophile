$ sudo cp /etc/mpd.conf.sav /dev/shm/mpd.conf && pkill mpd && sleep 1 && mpd-bg
$ mpc listall | grep -m1 노력 | mpc add && mpc play

1. sudo nano /etc/mpd.conf.sav
	buffer_before_play	"9.9985967986995619345437558093733132000409892%"
   sudo sed -i '/buffer_before_play/s/2%/24264544549%/' /etc/mpd.conf.sav

2. sudo nano /etc/sysctl.conf
	kernel.sched_wakeup_granularity_ns = 388355
	kernel.sched_shares_window = 346001
